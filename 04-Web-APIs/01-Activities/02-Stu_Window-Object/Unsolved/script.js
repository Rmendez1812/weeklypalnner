// TODO: What will the following line of code log? It will expose the window console
console.log(window);

// What will the following line of code log? It will show the document in which you can edit the page's DOM directly
console.log(window.document);

// TODO: What will the following line of code log? the elements in the document, ie header
console.log(document.documentElement);

// TODO: What will the following line of code log? the heading of the Document
console.log(document.head);

